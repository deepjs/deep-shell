#!/usr/bin/env node
/**
 * @author Gilles Coomans <gilles.coomans@gmail.com>
 */
require("deep-shell");
deep.sh().log("dpsh").pwd();
deep.context.cwd = __dirname;
var repl = require("repl")
.start({
	prompt: "> ",
	input: process.stdin,
	output: process.stdout
});